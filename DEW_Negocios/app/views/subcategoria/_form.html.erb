<%= form_for(@subcategorium) do |f| %>
  <% if @subcategorium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategorium.errors.count, "error") %> prohibited this subcategorium from being saved:</h2>

      <ul>
      <% @subcategorium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codcategoria %><br />
    <%= f.text_field :codcategoria %>
  </div>
  <div class="field">
    <%= f.label :codsubcategoria %><br />
    <%= f.text_field :codsubcategoria %>
  </div>
  <div class="field">
    <%= f.label :nombresubcategoria %><br />
    <%= f.text_field :nombresubcategoria %>
  </div>
  
   <div class="field">    
    <%= f.collection_select :categoria_id, Categorium.all, :id, :nombrecategoria, :prompt => true %>
	<!--Esto es un comentario -->
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

